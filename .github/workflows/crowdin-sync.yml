name: Sync Crowdin Translations

on:
  workflow_dispatch:

jobs:
  sync-translations:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # - name: Install Crowdin CLI
      #   run: |
      #     npm install -g @crowdin/cli

      - name: Download translations from Crowdin
        uses: crowdin/github-action@v1 # Use the latest version
        with:
          upload_sources: false # Set to false as we are only downloading
          upload_translations: false # Set to false as we are only downloading
          download_translations: true
          # Optional: Specify a branch to download translations to
          # localization_branch_name: "l10n_crowdin_action"
          # Optional: Create a pull request with the downloaded translations
          # create_pull_request: true
          # github_token: ${{ secrets.GITHUB_TOKEN }} # Required if creating a pull request
        env:
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_TOKEN }}

      # - name: Check for changes
      #   id: check_changes
      #   run: |
      #     if [[ -n $(git status --porcelain) ]]; then
      #       echo "has_changes=true" >> $GITHUB_OUTPUT
      #     else
      #       echo "has_changes=false" >> $GITHUB_OUTPUT
      #     fi

      # - name: Create Pull Request
      #   if: steps.check_changes.outputs.has_changes == 'true'
      #   uses: peter-evans/create-pull-request@v6
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     commit-message: "chore: update translations from Crowdin"
      #     branch: crowdin-translations
      #     delete-branch: true
      #     title: "🌐 Update translations from Crowdin"
      #     body: |
      #       ## Translation Updates

      #       This PR contains the latest translations from Crowdin.

      #       ### Changes
      #       - Updated translation files for all languages
      #       - Source: Crowdin Project
      #       - Generated automatically by GitHub Actions

      #       ### Review Checklist
      #       - [ ] Review translation changes
      #       - [ ] Verify JSON format is valid
      #       - [ ] Check for any missing keys

      #       ---
      #       *This PR was created automatically by the Crowdin sync workflow.*
      #     labels: |
      #       translations
      #       crowdin
      #       automated
      #     assignees: ${{ github.repository_owner }}
